<Window x:Class="PractWork4.Settings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PractWork4"
        mc:Ignorable="d"
        Title="Settings" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Content="Логин"/>
        <TextBox x:Name="LoginTextBox" Grid.Column="1"/>

        <Label Content="Пароль" Grid.Row="1"/>
        <TextBox x:Name="PasswordTextBox" Grid.Row="1" Grid.Column="1" />

        <Label Content="email" Grid.Row="2"/>
        <TextBox x:Name="EmailTextBox" Grid.Row="2" Grid.Column="1"/>
        
        <Button x:Name="SaveButton" Grid.Column="1" Grid.Row="3" Content="Сохранить" />

    </Grid>
</Window>
